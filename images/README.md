# Images

Drop these two files in this folder:

- `headshot.jpg` — your portrait (used as avatar + Find My pin).
- `nyc-map.png` — the NYC map screenshot (used inside the map bubble).

The HTML references them as `images/headshot.jpg` and `images/nyc-map.png`.
