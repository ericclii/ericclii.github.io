# ericclii.github.io

Personal site at [ericclii.github.io](https://ericclii.github.io).

Static HTML/CSS — no build step. Open `index.html` locally or serve the folder with any static server.

## Setup

Add two image files to `/images/`:

- `headshot.jpg`
- `nyc-map.png`
